<Window x:Class="EpicLibraryApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignDefaultFont}"
        Title="System Login" Height="550" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow">

    <Grid Background="#1E1E2C">
        <materialDesign:Card Margin="30" UniformCornerRadius="10" Padding="30" VerticalAlignment="Center" Background="White">
            <StackPanel>
                <materialDesign:PackIcon Kind="ShieldAccount" Width="50" Height="50" HorizontalAlignment="Center" Foreground="#7E57C2" Margin="0,0,0,10"/>
                <TextBlock Text="System Login" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#333333"/>
                <TextBlock Text="Africa University Engineering Library" FontSize="13" Foreground="#888888" HorizontalAlignment="Center" Margin="0,5,0,30" TextAlignment="Center" TextWrapping="Wrap"/>

                <ComboBox x:Name="RoleComboBox" materialDesign:HintAssist.Hint="Select your role" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,20" Foreground="#333333">
                    <ComboBoxItem Content="Student" IsSelected="True"/>
                    <ComboBoxItem Content="Staff"/>
                </ComboBox>

                <TextBox x:Name="UsernameTextBox" materialDesign:HintAssist.Hint="Student / Staff ID" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,20" Foreground="#333333"/>

                <PasswordBox x:Name="PasswordBox" materialDesign:HintAssist.Hint="Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,0,0,30" Foreground="#333333"/>

                <TextBlock x:Name="ErrorMessage" Text="Invalid ID, Password, or Role." Foreground="#E53935" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,10" Visibility="Collapsed"/>

                <Button Content="LOGIN" Height="45" Click="LoginButton_Click" Background="#7E57C2" BorderBrush="#7E57C2" Foreground="White" materialDesign:ButtonAssist.CornerRadius="8"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>